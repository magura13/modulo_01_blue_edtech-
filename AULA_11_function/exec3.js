const prompt = require(`prompt-sync`)();

const temp = prompt(`Enter if the temperature is in [C]elsius, [F]ahrenheit or [K]elvin: `)
const number = +prompt(`Enter the temperature: `)

function temperature(number, temp){
const answer = prompt(`Enter the temperature scale to return in [C]elsius, [F]ahrenheit or [K]elvin:`)
    
}

//transformar toda informação no prompt temp para Celsius
//serão varios if e else

